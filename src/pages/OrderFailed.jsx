import React from 'react';

const OrderFailed = () => (
  <div className="min-h-screen flex items-center justify-center">
    <h1 className="text-red-500 text-2xl">Payment Failed or Canceled</h1>
    {/* Add more details or links to retry */}
  </div>
);

export default OrderFailed;
